<div class="card-header">
  <div class="card-title">
    <h4 class="mb-0">{{ $title }}</h4>
    @if (!empty($description))
      <p class="text-muted my-2">{{ $description }}</p>
    @endif
  </div>
</div>
